require 'rails_helper'

describe "Viewing the list of items" do
  
  # it "shows the items" do
  #   item1 = Item.create(item_attributes)
  #   item2 = Item.create(item_attributes)
  #   item3 = Item.create(item_attributes)

  #   visit items_url

  #   expect(page).to have_text("Inventory")
  #   expect(page).to have_text(item1.name)
  #   expect(page).to have_text(item2.name)
  #   expect(page).to have_text(item3.name)

  #   expect(page).to have_text(item1.variety)
  #   expect(page).to have_text(item1.count)
  # end
end