<article id="user">
  <header>
    <h1><%= @user.name %></h1>
    <h2><%= mail_to(@user.email) %></h2>
  </header>
<!--   <nav>
    <%= link_to "Edit Account", edit_user_path(@user), class: 'btn btn-warning' %>
    <%= link_to "Delete Account", user_path(@user), class: 'btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' } %>
  </nav> -->
</article>